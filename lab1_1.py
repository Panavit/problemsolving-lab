# -*- coding: utf-8 -*-
"""Lab1-1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1BtYc48D11jtE0QhH1yeG2yzyL5KfsvRi

# **Lab1-1** ถ้าปัญหาคือ...การเขียนโปรแกรมเครื่องคิดเลข

1. รับค่าตัวเลข 2 ตัว พร้อมกำหนดเป็น **float**
"""

#ตัวอย่างผลลัพธ์

#รับค่าตัวเลข 2 ตัว
num1_str = input(str())
num2_str = input(str())

#แปลงเป็น float
num1 = float(num1_str)
num2 = float(num2_str)

print(f"ป้อนตัวเลขแรก: {num1}")
print(f"ป้อนตัวเลขที่สอง: {num2}")

"""2. รับค่าตัวดำเนินการ (operator เช่น +, -, *, /, %)"""

#ตัวอย่างผลลัพธ์

op = (input("(+, -, *, /, %):"))

"""3. คำนวณ โดยใช้ **if-else statements** ในการตรวจสอบตัวดำเนินการ และดำเนินการกระทำตามเงื่อนไข
- มีการแจ้งเตือน กรณี ไม่สามารถหารด้วยศูนย์ได้
- มีการแจ้งเตือน กรณี ตัวดำเนินการไม่ถูกต้อง
"""

if op == '+':
    result = num1 + num2
elif op == '-':
     result = num1 + num2
elif op == '*':
     result = num1 * num2
elif op == '/':
    if num2 == 0:
        print("ข้อผิดพลาด: ไม่สามารถหารด้วยศูนย์ได้")
    else:
        result = num1 / num2
elif op == '%':
    result = num1 % num2
else:
    print("ข้อผิดพลาด ข้อมูลไม่ถูกต้อง")

print(result)

"""4. แสดงผลลัพธ์"""

#ตัวอย่างผลลัพธ์

print(f"ผลลัพธ์: {num1} {op} {num2} = {result}")

"""# **ให้เขียนในแบบของการรับค่าผ่านฟังก์ชั่น**

1. เขียนฟังก์ชั่นรับค่าตัวเลข **get_number_input()**  (กรณีที่ไม่ใช่ตัวเลขให้วนรับใหม่ได้เรื่อยๆ)
"""

def get_number_input(prompt):
    while True:
        try:
            num_str = input(prompt)
            return float(num_str)
        except ValueError:
            print("ข้อผิดพลาด: คำที่ป้อนไม่ใช่ตัวเลขที่ถูกต้อง กรุณาลองใหม่")

"""ทดสอบเรียกใช้งานฟังก์ชั่น **get_number_input()**"""

#ตัวอย่างผลลัพธ์

#เขียนโค้ดตรงนี้
num1 = get_number_input("ป้อนตัวเลขแรก:")
num2 = get_number_input("ป้อนตัวเลขสอง:")

"""2. เขียนฟังก์ชั่นรับค่าตัวดำเนินการ **get_operator_input()** (กรณีไม่ใช่ +, -, *, /, หรือ % ให้วนรับใหม่)"""

def get_operator_input(prompt):
    while True:
        op = input(prompt)
        if op in valid_op:
            return op
        else:
            print("ข้อผิดพลาด: ตัวดำเนินการไม่ถูกต้อง กรุณาใช่ +, -, *, /, หรือ %")

"""ทดสอบเรียกใช้งานฟังก์ชั่นทั้ง 2 ต่อเนื่องกัน
- รับค่าตัวเลขแรก เรียกใช้ **get_number_input()**
- รับค่าตัวเลขที่สอง เรียกใช้ **get_number_input()**
- รับค่าตัวดำเนินการ เรียกใช้ **get_operator_input()**
"""

#ตัวอย่างผลลัพธ์

num1 = get_number_input("ป้อนตัวเลขแรก: ")
num2 = get_number_input("ป้อนตัวเลขที่สอง: ")
op4 = get_operator_input("ป้อนตัวดำเนินการ (+, -, *, /, %): ")

"""3.1 เขียนฟังก์ชั่นคำนวณ **calculate()** โดยใช้ **if-else statements** ในการตรวจสอบตัวดำเนินการ และดำเนินการกระทำตามเงื่อนไข"""

def calculate(num1, num2, operator):
    if operator == '+':
        result = num1 + num2
    elif op == '/':
            if num2 == 0:
                print("ข้อผิดพลาด: ไม่สามารถหารด้วยศูนย์ได้!")
                return None
            return num1 / num2
    return None

"""ทดสอบเรียกใช้งานฟังก์ชั่นทั้ง 3 ต่อเนื่องกัน
- รับค่าตัวเลขแรก เรียกใช้ **get_number_input()**
- รับค่าตัวเลขที่สอง เรียกใช้ **get_number_input()**
- รับค่าตัวดำเนินการ เรียกใช้ **get_operator_input()**
- คำนวณ เรียกใช้ **calculate()**

และแสดงผลลัพธ์
"""

#ตัวอย่างผลลัพธ์

#เขียนโค้ดตรงนี้
num1 = get_number_input("ป้อนตัวเลขแรก: ")
num2 = get_number_input("ป้อนตัวเลขที่สอง: ")
op4 = get_operator_input("ป้อนตัวดำเนินการ (+, -, *, /, %): ")

"""3.2 เขียนฟังก์ชั่นคำนวณ **calculate()** โดยใช้ **match-case (switch case)** ในการตรวจสอบตัวดำเนินการ และดำเนินการกระทำตามเงื่อนไข"""

def calculate2(num1, num2, operator):
   match operator:
    case '+':
        return num1 + num2
    case '/':
        if num2 == 0:
            print("ข้อผิดพลาด: ไม่สามารถหารด้วยศูนย์ได้!")
            return None
       return num1 / num2
    case _:
        return None

"""ทดสอบเรียกใช้งานฟังก์ชั่นทั้ง 3 ต่อเนื่องกัน
- รับค่าตัวเลขแรก เรียกใช้ get_number_input()
- รับค่าตัวเลขที่สอง เรียกใช้ get_number_input()
- รับค่าตัวดำเนินการ เรียกใช้ get_operator_input()
- คำนวณ เรียกใช้ calculate()

และแสดงผลลัพธ์
"""

#ตัวอย่างการเรียกใช้

#เขียนโค้ดตรงนี้

"""# **สร้าง Web App**

> **Streamlit + ngrok** สำหรับสร้าง Web App ใน Google Colab

- **Streamlit** ถูกออกแบบมาเพื่อเปลี่ยนสคริปต์ Python ธรรมดาให้เป็นเว็บแอปที่สวยงามและโต้ตอบได้ โดยไม่ต้องเขียน HTML, CSS หรือ JavaScript เลย
- **ngrok** ใช้เพื่อ
   - ให้แอปพลิเคชันที่รันอยู่ในเซสชัน Colab สามารถมี Public URL ที่สามารถแชร์ให้เพื่อนๆ หรือตัวคุณเองจากอุปกรณ์อื่นเข้าใช้งานได้
   - ช่วยให้เราสามารถทดสอบและแสดงผลงานเว็บแอปพลิเคชันของเราได้อย่างรวดเร็ว โดยไม่ต้องเสียเวลาติดตั้งและตั้งค่าเซิร์ฟเวอร์จริง

1. installing **streamlit** and **pyngrok** libraries
"""

pip install streamlit pyngrok

"""2. สร้างไฟล์ .py"""

# Commented out IPython magic to ensure Python compatibility.
# %%writefile calculator.py
# #เมื่อรันเสร็จจะมีไฟล์ calculator เกิดขึ้นมา
# import streamlit as st
# 
# def get_number_input(label):
#     return st.number_input(label, value=0.0, step=0.1, format="%.2f")
# 
# def get_operator_input(label):
#     valid_operators = ['+', '-', '*', '/', '%']
#     return st.selectbox(label, valid_operators)
# 
# def calculate(num1, num2, operator):
#     if operator == '+':
#         return num1 + num2
#     elif operator == '-':
#         return num1 - num2
#     elif operator == '*':
#         return num1 * num2
#     elif operator == '/':
#         if num2 == 0:
#             st.error("ข้อผิดพลาด: ไม่สามารถหารด้วยศูนย์ได้!")
#             return None
#         return num1 / num2
#     elif operator == '%':
#         if num2 == 0:
#             st.error("ข้อผิดพลาด: ไม่สามารถหารด้วยศูนย์ได้!")
#             return None
#         return num1 % num2
#     return None # Should not be reached if operator is validated
# 
# st.title('My Calculator')
# 
# # Get inputs from the user
# num1 = get_number_input("ป้อนตัวเลขแรก:")
# num2 = get_number_input("ป้อนตัวเลขที่สอง:")
# operator = get_operator_input("เลือกตัวดำเนินการ:")
# 
# # Perform calculation when the button is clicked
# if st.button('คำนวณ'):
#     result = calculate(num1, num2, operator)
#     if result is not None:
#         st.success(f"ผลลัพธ์: {num1} {operator} {num2} = {result}")



"""
3. Login **ngrok** ที่ https://ngrok.com/ and copy your **authtoken**"""

!ngrok authtoken 36GtzYVj3iFNImuus7chWUuTJCI_5QcPYqjWB4B3fKh9sPwaa

from pyngrok import ngrok

ngrok.kill()  # ปิด tunnel เก่า

public_url = ngrok.connect(8501)
print("🌍 Open your app here:", public_url)

"""4. **รัน app Streamlit** ที่สร้างขึ้นมา
การบอกให้ Colab รันไฟล์ calculator.py ด้วย Streamlit บนพอร์ต 8501
"""

!streamlit run calculator.py --server.port 8501 &

"""# การ Deploy ไปยัง **Streamlit Cloud**

1. ใช้ %%writefile เพื่อสร้างไฟล์ requirements.txt และเพิ่ม library ที่ใช้ในงานเราลงไป ถ้างานเรางานนี้ที่ต้องใช้ก็จะมีแค่ streamli
"""

# Commented out IPython magic to ensure Python compatibility.
# %%writefile requirements.txt
# streamlit

"""2. Download file calculator.py และ requirements.txt ลงเครื่อง

3. สร้าง GitHub Repository และ upload file calculator.py และ requirements.txt ไปใส่ไว้

4. ไปที่ https://share.streamlit.io/
login ด้วยบัญชี github
"""

from google.colab import drive
drive.mount('/content/drive')

"""5. Create app"""